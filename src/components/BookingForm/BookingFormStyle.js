import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(theme => ({
  panelHeader: {
    backgroundColor: "#bdbdbd"
  },
  panel: {
    width: "100%",
    margin: "5% auto"
  },
  button: {
    textAlign: "right"
  },
  buttonSec: {
    marginRight: 20
  }
}));
